version: "2"

run:
  go: "1.22"
  timeout: 5m

linters:
  default: all
  disable:
    # Line length - we'll use 80 char max but not enforce via linter.
    # Trust developers to keep lines readable.
    - lll

    # Allow package-level declarations such as the version constant.
    - gochecknoglobals

    # Allow short variable names.
    - varnamelen

    # Allow TODOs in code.
    - godox

    # Init functions are allowed.
    - gochecknoinits

    # Deprecated/problematic linters.
    - bodyclose
    - contextcheck
    - nilerr
    - noctx
    - rowserrcheck
    - sqlclosecheck
    - tparallel
    - unparam
    - wastedassign

    # Whitespace style conflicts.
    - wsl
    - wsl_v5

    # Allow default empty values in structs.
    - exhaustruct

    # Allow tests in same package.
    - testpackage

    # Cognitive complexity metrics - not useful for this project.
    - gocognit
    - gocyclo
    - maintidx
    - cyclop

    # Allow returning interfaces.
    - ireturn

    # Don't require error wrapping.
    - wrapcheck

    # Allow dynamic errors.
    - err113

    # We use ErrXXX naming.
    - errname

    # Allow returning multiple nils.
    - nilnil

    # Magic numbers OK in some contexts.
    - mnd

    # Parallel tests not required.
    - paralleltest

    # Function ordering is author's choice.
    - funcorder

    # Depguard not configured - disable to avoid false positives.
    - depguard

    # Performance suggestions - not critical for correctness.
    - prealloc
    - perfsprint

    # Test helpers in table-driven tests don't need t.Helper().
    - thelper

    # Named returns are fine in some contexts.
    - nonamedreturns

    # Inline error handling is idiomatic in some cases.
    - noinlineerr

    # Return after newline is a style preference.
    - nlreturn

    # intrange suggestions not critical.
    - intrange

    # Testify usage preferences - not critical.
    - testifylint

    # usetesting suggestions for t.TempDir() - not critical.
    - usetesting

    # Exhaustive switch - we handle default cases.
    - exhaustive

    # Nested if complexity - acceptable in some contexts.
    - nestif

  settings:
    dupl:
      threshold: 150

    funlen:
      lines: 150
      statements: 60

    nestif:
      min-complexity: 8

    # Kong tags read best in the order they affect parsing: what the field
    # is, then how it is spelled, then its help text. Column-aligning them
    # is pointless when one tag holds a whole sentence.
    tagalign:
      align: false
      order:
        - cmd
        - arg
        - name
        - short
        - required
        - optional
        - placeholder
        - default
        - help

    tagliatelle:
      case:
        rules:
          json: snake

  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling

    rules:
      - linters:
          - dupl
          - funlen
          - gosec
        path: _test\.go

formatters:
  enable:
    - gci
    - gofmt
    - goimports
